spring.datasource.url: jdbc:h2:mem:DBNAME;MODE=PostgreSQL
spring.datasource.username: sa
spring.datasource.password:
spring.datasource.driver-class-name: org.h2.Driver

flyway.url: "jdbc:h2:mem:DBNAME;MODE=PostgreSQL;INIT=RUNSCRIPT FROM 'classpath:flyway_init.sql';DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1"
flyway.user: sa
flyway.password:
flyway.baseline-on-migrate: true